# Arc testnet RPC URL (chain id 5042002).
# Get yours: `arc-canteen rpc-url --export` (Canteen CLI) or any public Arc RPC.
RPC=https://...

# Deployer private key for `forge script ... --broadcast`.
# Fund the address from the Arc testnet faucet first.
DEPLOYER_PRIVATE_KEY=0x...

# After deploy, paste the printed registry address here so the Python example
# can read it.
MERKLE_ANCHOR_ADDRESS=0x...
